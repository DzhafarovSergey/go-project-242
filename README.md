### Hexlet tests and linter status:
[![Actions Status](https://github.com/DzhafarovSergey/go-project-242/actions/workflows/hexlet-check.yml/badge.svg)](https://github.com/DzhafarovSergey/go-project-242/actions)
# hexlet-path-size

Утилита командной строки для определения размера файлов и директорий с поддержкой рекурсивного подсчета, человекочитаемого формата и скрытых файлов.

## Демонстрация

[![asciicast](https://asciinema.org/a/9bW9bOVtKqWO3El1)](https://asciinema.org/a/9bW9bOVtKqWO3El1)

*Нажмите на изображение, чтобы посмотреть демонстрацию работы утилиты*

## Возможности

- 📁 Определение размера файлов и директорий
- 🔄 Рекурсивный подсчет (включая вложенные директории)
- 👁️ Поддержка скрытых файлов и директорий
- 📊 Человекочитаемый формат (автоматический выбор единиц: B, KB, MB, GB, TB, PB, EB)
- ⚡ Быстрая работа с большими структурами директорий
- ✅ Полное покрытие тестами

## Установка

### Способ 1: Сборка из исходников

```bash
# Клонирование репозитория
git clone <repository-url>
cd go-project-242

# Сборка
make build
# или
go build -o bin/hexlet-path-size ./cmd/hexlet-path-size
```

### Способ 2: Установка через go install

```bash
go install github.com/your-username/hexlet-path-size@latest
```

## Использование

```bash
./bin/hexlet-path-size [options] <path>
```

### Опции

| Флаг | Короткая версия | Описание |
|------|----------------|----------|
| `--human` | `-h` | Человекочитаемый формат (автовыбор единиц) |
| `--all` | `-a` | Включать скрытые файлы и директории |
| `--recursive` | `-r` | Рекурсивный подсчет для директорий |

### Примеры

#### Базовые примеры
```bash
# Размер файла в байтах
./bin/hexlet-path-size data.csv

# Человекочитаемый формат
./bin/hexlet-path-size -h data.csv

# Директория (только файлы первого уровня)
./bin/hexlet-path-size project/
```

#### Продвинутые примеры
```bash
# Рекурсивный подсчет директории
./bin/hexlet-path-size -r project/

# Включая скрытые файлы
./bin/hexlet-path-size -a project/

# Все возможности вместе
./bin/hexlet-path-size -h -a -r project/

# Явное указание формата в байтах
./bin/hexlet-path-size --human=false output.dat
```

#### Работа с тестовыми данными
```bash
# Обычный файл
./bin/hexlet-path-size tests/fixtures/testdata/data/ndata.csv

# Скрытый файл (требует флаг -a)
./bin/hexlet-path-size -a tests/fixtures/testdata/data/.ex_data.txt

# Директория со скрытыми файлами
./bin/hexlet-path-size -a tests/fixtures/testdata/data/
```

## Примеры вывода

```
# Без флагов (байты)
138550B	data.csv

# С флагом -h (человекочитаемый)
135.3KB	data.csv

# 24MB файл
25165824B	output.dat        # без флага
24MB	output.dat              # с флагом -h

# Директория
10B	/path/to/directory         # без рекурсии
150B	/path/to/directory        # с рекурсией (-r)
```

## Тестирование

Проект включает полный набор тестов:

```bash
# Запуск всех тестов
make test
go test ./tests/...

# Запуск с подробным выводом
go test -v ./tests/...

# Проверка покрытия кода
make test-coverage
```

## Структура проекта

```
.
├── cmd/hexlet-path-size/     # Основная программа
│   └── main.go
├── internal/
│   ├── cli/                  # Логика командной строки
│   │   └── cli.go
│   └── pathsize/             # Бизнес-логика
│       └── pathsize.go
├── tests/                    # Тесты
│   ├── fixtures/            # Тестовые данные
│   │   └── testdata/
│   │       └── data/
│   │           └── .keep
│   └── path_size_test.go
├── Makefile                  # Управление сборкой
└── README.md
```

## Makefile команды

```bash
make build      # Сборка проекта
make test       # Запуск тестов
make lint       # Проверка кода
make clean      # Очистка артефактов сборки
make all        # Сборка + тесты
```
